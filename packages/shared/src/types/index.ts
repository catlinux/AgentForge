/**
 * Types shared between AgentForge Core and the Secrets Broker.
 *
 * Intentionally minimal for now — this grows as message shapes are defined in later phases
 * (Fase 6 — Secrets Broker). Placeholder to establish the package's shape (DEC-012).
 */
export type AgentForgeComponent = "core" | "secrets-broker";
