// AgentForge Core — entry point placeholder (no wiring/CLI yet).
// Policy Engine and MCP/hooks integration are added in later, explicitly authorized phases
// (see ROADMAP.md). The Tool Registry (Fase 3) is implemented in ./registry.
export * from "./registry/index.js";
