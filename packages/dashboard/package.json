{
  "name": "@agentforge/dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b && node -e \"const fs=require('fs');fs.cpSync('src/public','dist/public',{recursive:true})\"",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@agentforge/shared": "workspace:*",
    "@agentforge/core": "workspace:*",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
